/* c3.1.js — this page has no widget-specific JS of its own.
   Case-study modals and the .case-photo image lightbox are handled
   globally by curriculum.js, so nothing page-specific is needed here.
   Kept as an (intentionally empty) file so the <script> tag stays valid. */
